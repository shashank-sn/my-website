# Apply to shashank-sn/shashank-sn

This folder contains the neofetch-style GitHub profile README. Copy into the profile repo:

```bash
git clone git@github.com:shashank-sn/shashank-sn.git
cd shashank-sn
git checkout -b username/neofetch-profile-readme-7698
cp -r /path/to/github-profile-readme/{assets,scripts,.github,README.md} .
git add -A
git commit -m "Add neofetch-style GitHub profile README"
git push -u origin username/neofetch-profile-readme-7698
```

Or apply the patch from `profile-neofetch-patch/` inside a clone of `shashank-sn/shashank-sn`.
